# Unsorted WinRT Notes

Intake rule: split future links into raw WinRT ABI/activation, XAML/WinUI hosting, or packaged-identity/lifecycle behavior before expanding this file. Mixing those topics is how WinRT notes become generic and misleading.
