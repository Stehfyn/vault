# Unsorted

No loose Game Bar links are parked here. New material should usually become one of three concrete entry types: widget lifecycle (`XboxGameBarSamples`), Xbox service/auth (`xbox-live-api`, `XblIdpAuthManager.h`), or legacy shell/game identity (`gameux.h`, `Game Bar Presence Writer`).
