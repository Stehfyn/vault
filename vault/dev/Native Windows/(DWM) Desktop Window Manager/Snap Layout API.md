# Snap Layout API

There is no public API to programmatically invoke Snap Layouts; you can only opt in (e.g., via `WM_NCHITTEST`) or simulate the Win+Z UI.

## Snippet
> No, Microsoft have never released an API for "snap" functionality.

## References
- https://stackoverflow.com/questions/77523400/is-there-an-api-to-programmatically-invoke-snap-layout
