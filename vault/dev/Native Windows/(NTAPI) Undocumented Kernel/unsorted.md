# Unsorted NTAPI Notes

Use this only for native API, object-manager, process/thread, token, syscall, or undocumented-structure references that have not yet been tied to a concrete subsystem. Promote items once the note can distinguish public Win32 wrapper behavior from native contract, private structure layout, or kernel observation point.
