# ip2string.h (Duplicate Stub)

Duplicate/mistitled stub for `ip2string.h`; the original dump captured the include line rather than the canonical header entry. Keep this note only as a redirect so backlinks do not silently die. The real topic is `ip2string.h.md`: `RtlIpv4*` / `RtlIpv6*` parsing and formatting from `ntdll.dll`, useful when bootstrap or service code wants address conversion without Winsock startup.

## References
- <https://learn.microsoft.com/en-us/windows/win32/api/ip2string/>
