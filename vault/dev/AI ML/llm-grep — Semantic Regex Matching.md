<!-- generated-from-dump2 -->
# llm-grep — Semantic Regex Matching

Match lines using both classic and semantic regular expressions with LLM.

```python
import torch
model.eval()
with torch.inference_mode():
    out = model(x)
```

## References
- <https://github.com/vagos/llm-grep>
