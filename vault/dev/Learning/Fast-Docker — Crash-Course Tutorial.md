<!-- generated-from-dump2 -->
# Fast-Docker — Crash-Course Tutorial

One-stop crash-course on Docker covering everything from `Dockerfile` syntax to multi-stage builds, Compose, networks, volumes, Swarm/Stack/Service, and real-world scenarios (Java app, Python app, MySQL + WordPress). Markdown-only; useful as a single bookmark that surveys the whole surface area without bouncing across docs.

```text
# Index (paraphrased from the repo's README):
01. Docker Architecture
02. Installation
03. Container basics: run, exec, stop, rm
04. Image basics: build, tag, push, pull
05. Dockerfile in depth + multistage
06. Volumes & bind mounts
07. Networks (bridge / host / overlay)
08. Compose: multi-container apps
09. Swarm: stack, service, secrets, configs
10. Real-world scenarios + troubleshooting
```

## References
- <https://github.com/omerbsezer/Fast-Docker>
