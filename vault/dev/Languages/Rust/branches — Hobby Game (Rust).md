<!-- generated-from-dump2 -->
# branches — Hobby Game (Rust)

Small Rust game by notgull. Not a library - a finished hobby project. Worth a look only if you're collecting examples of "complete tiny Rust game with windowing + rendering + game loop in one repo." Use as a reference of how the author wires winit + a software renderer (or whatever crate they chose) end-to-end.

```text
# To play / inspect:
$ git clone https://github.com/notgull/branches
$ cd branches && cargo run --release
```

## References
- <https://github.com/notgull/branches>
